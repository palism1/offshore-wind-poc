# Local-only files
RESUME_LOG.md

# Python
__pycache__/
*.py[cod]
.venv/
.env

# Data (raw pulls stay local; provenance lives in the DB)
data/
*.parquet
*.csv

# OS
.DS_Store

# Local tooling — kept out of the project entirely
local-tooling/
.agents/
