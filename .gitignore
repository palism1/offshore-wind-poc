# Local-only files
RESUME_LOG.md

# Python
__pycache__/
*.py[cod]
.venv/
.env

# Data (raw pulls stay local; provenance lives in the DB)
data/
*.parquet
*.csv

# OS
.DS_Store

# Local backups and outgoing drafts (never commit — the bundle holds pre-rewrite history)
*.bundle
2026-*_project_update.md
